{
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export",
    "deploy": "next build && next export && node bin/deploy.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "next": "latest",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "@babel/core": ">=7.0.0 <8.0.0",
    "@types/marked": "^4.0.1",
    "gh-pages": "^3.2.3",
    "marked": "^4.0.10",
    "sass": "^1.49.0"
  },
  "peerDependencies": {
    "@babel/core": ">=7.0.0 <8.0.0"
  }
}
